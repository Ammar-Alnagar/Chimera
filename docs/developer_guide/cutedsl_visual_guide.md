# CuteDSL Visual Guide

## Understanding Chimera's Kernel Strategy Through Diagrams

This companion document provides visual explanations of how CuteDSL works within Chimera.

## 1. Kernel Abstraction Layers

```mermaid
flowchart TB
    subgraph "Level 4: Application"
        A1[LLM Inference Request]
    end

    subgraph "Level 3: Python API"
        B1[cutedsl_fp8_blockwise_scaled_mm]
        B2[cutedsl_mla_decode]
        B3[cutedsl_es_grouped_mm]
    end

    subgraph "Level 2: Torch Bindings"
        C1[torch.ops.sgl_kernel.*]
        C2[PyTorch Dispatch]
    end

    subgraph "Level 1: CuteDSL Implementation"
        D1[CuteDSL Functors]
        D2[Layout Algebra]
        D3[Thread Mapping]
    end

    subgraph "Level 0: Hardware"
        E1[Tensor Core Instructions]
        E2[Shared Memory]
        E3[Register File]
    end

    A1 --> B1
    A1 --> B2
    A1 --> B3
    B1 --> C1
    B2 --> C1
    B3 --> C1
    C1 --> C2
    C2 --> D1
    C2 --> D2
    C2 --> D3
    D1 --> E1
    D2 --> E1
    D3 --> E2
    D3 --> E3

    style B1 fill:#4CAF50,color:#fff
    style B2 fill:#4CAF50,color:#fff
    style B3 fill:#4CAF50,color:#fff
    style D1 fill:#2196F3,color:#fff
    style D2 fill:#2196F3,color:#fff
    style D3 fill:#2196F3,color:#fff
```

## 2. Data Flow: From Python to GPU

```mermaid
sequenceDiagram
    participant User as Python User Code
    participant Wrapper as Python Wrapper
    participant Torch as PyTorch C++
    participant Dispatcher as Kernel Dispatcher
    participant CuteDSL as CuteDSL Functor
    participant GPU as NVIDIA GPU

    User->>Wrapper: cutedsl_fp8_gemm(A, B, scales_a, scales_b)
    Note over User,Wrapper: High-level API call
    
    Wrapper->>Wrapper: Validate tensor shapes
    Wrapper->>Wrapper: Check dtype compatibility
    
    Wrapper->>Torch: torch.ops.sgl_kernel.cutedsl_gemm()
    Note over Wrapper,Torch: Cross language boundary
    
    Torch->>Dispatcher: Dispatch to kernel
    
    alt First Call (JIT)
        Dispatcher->>CuteDSL: Instantiate template
        CuteDSL->>CuteDSL: Generate PTX for GPU arch
        CuteDSL-->>Dispatcher: Compiled kernel
    end
    
    Dispatcher->>CuteDSL: Launch configuration
    CuteDSL->>GPU: CUDA kernel launch
    
    Note over GPU: Tensor Core Execution
    GPU->>GPU: Load A, B from global memory
    GPU->>GPU: Apply scale factors
    GPU->>GPU: Matrix multiply accumulate
    GPU->>GPU: Store result to global memory
    
    GPU-->>CuteDSL: Kernel complete
    CuteDSL-->>Dispatcher: Return status
    Dispatcher-->>Torch: Result tensor
    Torch-->>Wrapper: torch.Tensor
    Wrapper-->>User: Output tensor
    
    Note over User,GPU: Total latency: 10-100 μs
```

## 3. CuteDSL vs Traditional CUDA

```mermaid
flowchart LR
    subgraph "Traditional CUDA Kernel"
        A1[Manual pointer arithmetic]
        A2[Explicit thread indexing]
        A3[Manual shared memory mgmt]
        A4[Architecture-specific intrinsics]
        A5[~500-1000 lines of code]
    end

    subgraph "CuteDSL Kernel"
        B1[Tensor layout abstraction]
        B2[Automatic thread mapping]
        B3[Compiler-managed shared memory]
        B4[Architecture-agnostic DSL]
        B5[~100-200 lines of code]
    end

    A1 -->|Abstracted by| B1
    A2 -->|Automated by| B2
    A3 -->|Managed by| B3
    A4 -->|Generated by| B4
    A5 -->|Reduced to| B5

    style A1 fill:#FF9800,color:#fff
    style A2 fill:#FF9800,color:#fff
    style A3 fill:#FF9800,color:#fff
    style A4 fill:#FF9800,color:#fff
    style A5 fill:#FF9800,color:#fff
    style B1 fill:#4CAF50,color:#fff
    style B2 fill:#4CAF50,color:#fff
    style B3 fill:#4CAF50,color:#fff
    style B4 fill:#4CAF50,color:#fff
    style B5 fill:#4CAF50,color:#fff
```

## 4. Kernel Fallback Strategy

```mermaid
flowchart TD
    Start[Kernel Request] --> Check1{CuteDSL<br/>Installed?}
    Check1 -->|No| Fallback1[Use torch.ops.sgl_kernel]
    Check1 -->|Yes| Check2{GPU Architecture<br/>Supported?}
    
    Check2 -->|No<br/>(e.g., Ampere)| Fallback1
    Check2 -->|Yes<br/>(e.g., Hopper)| Check3{Input Types<br/>Valid?}
    
    Check3 -->|No| Error[Raise Error]
    Check3 -->|Yes| Check4{Memory<br/>Available?}
    
    Check4 -->|No| Fallback2[Use Smaller Tiles<br/>or Fallback]
    Check4 -->|Yes| Execute[Execute CuteDSL Kernel]
    
    Execute --> Success{Success?}
    Success -->|Yes| Return[Return Result]
    Success -->|No| Retry[Retry with Fallback]
    
    Fallback1 --> Return
    Fallback2 --> Return
    Retry --> Return
    
    style Execute fill:#4CAF50,color:#fff
    style Return fill:#4CAF50,color:#fff
    style Fallback1 fill:#FF9800,color:#fff
    style Fallback2 fill:#FF9800,color:#fff
    style Error fill:#F44336,color:#fff
```

## 5. FP8 Blockwise GEMM Architecture

```mermaid
flowchart TB
    subgraph "Input Tensors"
        A["Tensor A (M×K)<br/>FP8 + Scale Factors"]
        B["Tensor B (N×K)<br/>FP8 + Scale Factors"]
    end

    subgraph "CuteDSL Kernel Stages"
        S1["Stage 1: Load<br/>Global → Shared Memory"]
        S2["Stage 2: Scale<br/>Apply block scales"]
        S3["Stage 3: Compute<br/>Tensor Core MMA"]
        S4["Stage 4: Store<br/>Shared → Global Memory"]
    end

    subgraph "Output"
        C["Tensor C (M×N)<br/>FP16/BF16"]
    end

    A --> S1
    B --> S1
    S1 --> S2
    S2 --> S3
    S3 --> S4
    S4 --> C

    style S1 fill:#2196F3,color:#fff
    style S2 fill:#2196F3,color:#fff
    style S3 fill:#4CAF50,color:#fff
    style S4 fill:#2196F3,color:#fff
```

## 6. Multi-Stage Pipelining

```mermaid
gantt
    title CuteDSL Kernel Pipelining (4 stages)
    dateFormat X
    axisFormat %L
    
    section Stage 0
    Load A0,B0 :a0, 0, 10
    Compute C0 :crit, a1, 10, 15
    
    section Stage 1
    Load A1,B1 :a2, 10, 20
    Compute C1 :crit, a3, 25, 40
    
    section Stage 2
    Load A2,B2 :a4, 20, 30
    Compute C2 :crit, a5, 40, 55
    
    section Stage 3
    Load A3,B3 :a6, 30, 40
    Compute C3 :crit, a7, 55, 70
    
    section Memory
    Async Copy :m1, 0, 70
```

## 7. Thread Hierarchy Mapping

```mermaid
flowchart TB
    subgraph "GPU Hierarchy"
        G1["GPU Device"]
        
        subgraph "Thread Block Cluster"
            B1["Block 0"]
            B2["Block 1"]
            B3["..."]
            B4["Block N"]
        end
        
        subgraph "Warp Group (per Block)"
            W1["Warp 0-3<br/>Tile 0"]
            W2["Warp 4-7<br/>Tile 1"]
        end
        
        subgraph "Warp (32 threads)"
            T1["Thread 0-31"]
        end
    end

    G1 --> B1
    G1 --> B2
    G1 --> B3
    G1 --> B4
    B1 --> W1
    B1 --> W2
    W1 --> T1

    style G1 fill:#2196F3,color:#fff
    style B1 fill:#4CAF50,color:#fff
    style B2 fill:#4CAF50,color:#fff
    style W1 fill:#FF9800,color:#fff
    style W2 fill:#FF9800,color:#fff
    style T1 fill:#F44336,color:#fff
```

## 8. Memory Movement in CuteDSL

```mermaid
flowchart LR
    subgraph "Memory Hierarchy"
        M1["Global Memory<br/>VRAM (GBs)"]
        M2["Shared Memory<br/>Per SM (KB-MB)"]
        M3["Registers<br/>Per Thread (KB)"]
        M4["Tensor Core<br/>Accumulators"]
    end

    subgraph "CuteDSL Operations"
        O1["TMA Load<br/>Global → Shared"]
        O2["Async Copy<br/>Shared → Register"]
        O3["WGMMA<br/>Register → Accumulator"]
        O4["Epilogue<br/>Accumulator → Global"]
    end

    M1 <-->|TMA| O1
    O1 <--> M2
    M2 <-->|Async Copy| O2
    O2 <--> M3
    M3 <-->|WGMMA| O3
    O3 <--> M4
    M4 <-->|Store| O4
    O4 <--> M1

    style M1 fill:#9C27B0,color:#fff
    style M2 fill:#2196F3,color:#fff
    style M3 fill:#FF9800,color:#fff
    style M4 fill:#4CAF50,color:#fff
```

## 9. MoE Expert Specialization Flow

```mermaid
flowchart TB
    subgraph "MoE Layer"
        A["Input Tokens<br/>(batch × hidden)"]
        B["Gating Network<br/>Top-K Selection"]
        C["Expert Routing<br/>Group by expert"]
    end

    subgraph "CuteDSL Grouped GEMM"
        D["Expert 0<br/>cutedsl_es_fp8_gemm"]
        E["Expert 1<br/>cutedsl_es_fp8_gemm"]
        F["Expert N<br/>cutedsl_es_fp8_gemm"]
    end

    subgraph "Output"
        G["Combine Expert Outputs<br/>(weighted sum)"]
    end

    A --> B
    B --> C
    C --> D
    C --> E
    C --> F
    D --> G
    E --> G
    F --> G

    style D fill:#4CAF50,color:#fff
    style E fill:#4CAF50,color:#fff
    style F fill:#4CAF50,color:#fff
```

## 10. Performance Comparison

```mermaid
xychart-beta
    title "FP8 GEMM Performance: CuteDSL vs Traditional"
    x-axis ["128²", "256²", "512²", "1024²", "2048²", "4096²"]
    y-axis "TFLOPS" 0 --> 500
    bar [50, 120, 280, 420, 480, 490]
    line [30, 80, 200, 320, 380, 400]
```

**Legend**:
- **Bar**: CuteDSL (CUTLASS 4.x)
- **Line**: Traditional CUDA kernels

## 11. Build and Deployment Pipeline

```mermaid
flowchart LR
    subgraph "Development"
        A1[Write CuteDSL Code]
        A2[Unit Tests]
        A3[Benchmark Tests]
    end

    subgraph "Build"
        B1[CMake Configure]
        B2[NVCC Compile]
        B3[PTX Generation]
        B4[Python Binding]
    end

    subgraph "Deployment"
        C1[Docker Build]
        C2[Wheel Package]
        C3[PyPI Publish]
    end

    subgraph "Runtime"
        D1[Install Package]
        D2[JIT Compile]
        D3[Execute Kernel]
    end

    A1 --> A2
    A2 --> A3
    A3 --> B1
    B1 --> B2
    B2 --> B3
    B3 --> B4
    B4 --> C1
    B4 --> C2
    C2 --> C3
    C3 --> D1
    C1 --> D1
    D1 --> D2
    D2 --> D3

    style A1 fill:#4CAF50,color:#fff
    style B2 fill:#2196F3,color:#fff
    style C1 fill:#FF9800,color:#fff
    style D3 fill:#4CAF50,color:#fff
```

## 12. Kernel Selection Decision Tree

```mermaid
flowchart TD
    Start[Kernel Operation] --> Op{Operation Type?}
    
    Op -->|GEMM| GEMM{Input Precision?}
    Op -->|Attention| Attn{Attention Type?}
    Op -->|MoE| MoE{Expert Format?}
    
    GEMM -->|FP8| FP8{Block Scaling?}
    GEMM -->|FP16/BF16| Standard[Use Standard GEMM]
    
    FP8 -->|Yes| FP8Block[cutedsl_fp8_blockwise_scaled_mm]
    FP8 -->|No| FP8PerToken[cutedsl_fp8_per_token_scaled_mm]
    
    Attn -->|MLA Decode| MLA[cutedsl_mla_decode]
    Attn -->|Standard| FlashAttn[Use Flash Attention]
    
    MoE -->|FP8 Blockwise| MoEFP8[cutedsl_es_fp8_blockwise_grouped_mm]
    MoE -->|SM100 MXFP8| MoEMX[cutedsl_es_sm100_mxfp8_blockscaled_mm]
    
    style FP8Block fill:#4CAF50,color:#fff
    style FP8PerToken fill:#4CAF50,color:#fff
    style MLA fill:#4CAF50,color:#fff
    style MoEFP8 fill:#4CAF50,color:#fff
    style MoEMX fill:#4CAF50,color:#fff
    style Standard fill:#2196F3,color:#fff
    style FlashAttn fill:#2196F3,color:#fff
```

## 13. Error Handling Flow

```mermaid
flowchart TD
    Start[Kernel Call] --> Try{Try Block}
    
    Try --> Execute[Execute CuteDSL Kernel]
    Execute --> Check{Success?}
    
    Check -->|Yes| Return[Return Result]
    
    Check -->|No| Catch{Exception Type?}
    
    Catch -->|ImportError| Log1[Log: CuteDSL not available]
    Catch -->|RuntimeError| Log2[Log: Kernel execution failed]
    Catch -->|CUDAError| Log3[Log: GPU error occurred]
    
    Log1 --> Fallback[Use Fallback Kernel]
    Log2 --> Fallback
    Log3 --> Fallback
    
    Fallback --> FallbackCheck{Fallback Success?}
    
    FallbackCheck -->|Yes| Return
    FallbackCheck -->|No| Raise[Raise Exception]
    
    style Return fill:#4CAF50,color:#fff
    style Fallback fill:#FF9800,color:#fff
    style Raise fill:#F44336,color:#fff
```

## 14. Complete Inference Pipeline

```mermaid
flowchart TB
    subgraph "Preprocessing"
        A1[Tokenize Input]
        A2[Embedding Lookup]
    end

    subgraph "Transformer Layers"
        B1["Layer 1<br/>Attention + CuteDSL GEMM"]
        B2["Layer 2<br/>Attention + CuteDSL GEMM"]
        B3["..."]
        B4["Layer N<br/>Attention + CuteDSL GEMM"]
    end

    subgraph "Postprocessing"
        C1[Layer Norm]
        C2[LM Head]
        C3[Softmax]
        C4[Token Selection]
    end

    A1 --> A2
    A2 --> B1
    B1 --> B2
    B2 --> B3
    B3 --> B4
    B4 --> C1
    C1 --> C2
    C2 --> C3
    C3 --> C4

    style B1 fill:#4CAF50,color:#fff
    style B2 fill:#4CAF50,color:#fff
    style B4 fill:#4CAF50,color:#fff
```

---

**Last Updated**: March 29, 2026
**Version**: Chimera v1.0
